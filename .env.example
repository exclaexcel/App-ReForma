# App ReForma - Variáveis de Ambiente
# Copie este arquivo para .env.local e preencha com seus valores

# Supabase (obrigatórias no app — a anon key é pública por desenho; RLS é a barreira)
NEXT_PUBLIC_SUPABASE_URL=seu_url_do_supabase_aqui
NEXT_PUBLIC_SUPABASE_ANON_KEY=sua_chave_anonima_aqui

# E2E Tests (Playwright)
E2E_EMAIL=usuario-teste@exemplo.com
E2E_PASSWORD=senha-teste-segura
PLAYWRIGHT_BASE_URL=http://localhost:3000
PLAYWRIGHT_RUN_ID=

# Só para e2e/helpers/cleanup.ts (Node). NUNCA prefixar NEXT_PUBLIC_.
# Não é usada pelo runtime do app (100% client + RLS).
# SUPABASE_SERVICE_ROLE_KEY=
