# Arquivo — dumps e referência

Material **histórico / rascunho**. Não é fonte de verdade do produto.

| Arquivo                  | O que é                               |
| ------------------------ | ------------------------------------- |
| `claude_*.md`            | Exports de contexto Claude            |
| `bagunca.md`             | Prompt de auditoria (rascunho)        |
| `despesas-auditoria.txt` | Notas soltas de auditoria de despesas |
| `setup-debug.png`        | Print de setup                        |

Estado atual: código + `supabase/migrations/` + docs em `produto/` / `operacao/`.
