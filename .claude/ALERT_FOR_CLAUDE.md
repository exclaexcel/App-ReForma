# ALERTA: Acesso ao Supabase — Atenção Claude

Por favor, atenção às operações sobre o projeto Supabase deste repositório.

- Projeto correto: `bhsvvpvfbszrcitjwxxl`
- NÃO execute comandos que alterem o banco remoto (por exemplo: `supabase link`, `supabase migration`, `supabase db push`, `supabase db reset`, `supabase start` com deploy remoto) sem autorização explícita do maintainer (Dany).
- Antes de qualquer link/alteração, confirme:
  1. que a sessão CLI está autenticada com a conta que tem acesso ao projeto;
  2. que o `project_ref` corresponde ao projeto acima.
- Não utilize chaves ou tokens encontrados em arquivos do repositório sem confirmar — peça o token ao maintainer ou utilize GitHub Secrets (`SUPABASE_ACCESS_TOKEN`, `SUPABASE_SERVICE_ROLE_KEY`).
- Se você já realizou operações com credenciais diferentes, informe imediatamente o maintainer e não aplique migrations adicionais até confirmação e backup.

Contato: peça autorização ao Dany neste workspace antes de prosseguir.
